name = "bad_docker"
version = "1.0.0"
project = "default"
plan = "free"

[code]
location = "code/"
python_application = "app:app"
health_check_endpoint = "/"
docker = "ghcr.io/cosmian/mse-pytorch:notexist"